<!DOCTYPE html>
<html>

<head>
    <title>Todo List</title>
    <link rel="stylesheet" type="text/css" href="/styles.css">
</head>

<body>
    <div class="container">
        <h1>Welcome, <%= user.username %>!</h1>
        <h2>Your Tasks:</h2>
        <ul>
            <% tasks.forEach((task, index)=> { %>
                <li>
                    <strong>
                        <%= task.title %>
                    </strong>
                    (Due: <%= task.dueDate %>)
                        <form action="/tasks/<%= index %>/delete" method="post" class="task-form delete-form">
                            <button class="delete" type="submit">X</button>
                        </form>
                        <form action="/tasks/<%= index %>/update" method="post" class="task-form">
                            <label for="title-<%= index %>">Title:</label>
                            <input type="text" id="title-<%= index %>" name="title" value="<%= task.title %>" required>
                            <label for="dueDate-<%= index %>">Due Date:</label>
                            <input type="date" id="dueDate-<%= index %>" name="dueDate" value="<%= task.dueDate %>"
                                required>
                            <button type="submit">Update</button>
                        </form>
                </li>
                <% }); %>
        </ul>
        <form action="/tasks" method="post">
            <h2>Add a Task:</h2>
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" required>
            <label for="dueDate">Due Date:</label>
            <input type="date" id="dueDate" name="dueDate" required>
            <button type="submit">Add Task</button>
        </form>
        <a href="/logout" class="logout-link">Logout</a>

    </div>
</body>

</html>