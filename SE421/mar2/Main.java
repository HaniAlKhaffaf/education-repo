package mar2;

public class Main {
    public static void main(String[] args) {
        
        // This class we will focus on OOA and OOD
        // the model used called CRC card, its handy and easy to use
        // The CRC has Responsibility and Collaboration, the 
        // Responsibilities are what the class knows or does.
        // Collaborations are how classes react with eachother

        // CRC is done before writing the code, so you have to draw and design
        // a CRC card before actually going into the coding aspect.

        // two homeworks, one in moodle uploaded, and the other is as the following
        // Homework --> a print method that can print anything up to desire,
        // should be a neat design that only has one print.
        
    }
}
